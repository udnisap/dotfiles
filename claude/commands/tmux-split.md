---
allowed-tools: Bash(bash ~/.claude/scripts/tmux-split.sh:*)
description: Open a tmux split pane in Claude's current working directory (the active worktree)
argument-hint: "[right|down|window]"
---

!`bash ~/.claude/scripts/tmux-split.sh $ARGUMENTS`
